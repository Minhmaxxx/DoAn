[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    model: requires the benchmark-selected Baseline B checkpoint and image fixtures
    live: calls an external LLM API and is disabled unless explicitly requested
